import { fiamAttributes } from "../models/fiam.model";

const getFiamDataOfRequest = (rawBody: any): fiamAttributes => {
	// todo: obtener los datos del body
	return rawBody;
};

export default getFiamDataOfRequest;
