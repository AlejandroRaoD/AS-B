import { studentAttributes } from "../models/student.model";

const getStudentDataOfRequest = (rawBody: any): studentAttributes => {
	// todo: obtener los datos del body
	return rawBody;
};

export default getStudentDataOfRequest;
