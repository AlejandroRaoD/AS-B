import { programaAttributes } from "../models/programa.model";

const getProgramaDataOfRequest = (rawBody: any): programaAttributes => {
	// todo: obtener los datos del body
	return rawBody;
};

export default getProgramaDataOfRequest;
