import { catedraAttributes } from "../models/catedra.model";

const getCatedraDataOfRequest = (rawBody: any): catedraAttributes => {
	// todo: obtener los datos del body
	return rawBody;
};

export default getCatedraDataOfRequest;
