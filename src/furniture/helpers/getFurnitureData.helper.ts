import { furnitureAttributes } from "../models/furniture.model";

const getFurnitureDataOfRequest = (rawBody: any): furnitureAttributes => {
	// todo: obtener los datos del body
	return rawBody;
};

export default getFurnitureDataOfRequest;
