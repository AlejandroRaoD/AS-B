export enum Gender {
	Masculine = "m",
	Feminine = "f",
	Other = "other",
}

export enum BusinessPosition {
	fiam = "fiam",
	other = "other",
}

// ****************************************************************************
// 										         usuarios
// ****************************************************************************

export enum UserRoles {
	student = "student",
	admin = "admin",
}

export enum UserPermissions {
	edit = "edit",
}
